[project]
name = "blast-radius-engine"
version = "0.1.0"
requires-python = "==3.13.*"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "uvicorn>=0.30.0",
    "networkx>=3.3",
    "pydantic>=2.8.0",
]

[tool.fastapi]
entrypoint = "app.main:app"
